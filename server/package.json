{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:ci": "ts-node ./bin/www.ts",
    "dev:ci": "DEBUG=* ts-node ./bin/www.ts",

    "lint": "eslint . --ext .ts",

    "start": "node index.js",
    "dev": "nodemon index.js",
    
    "test": "vitest",
    "test:run": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run --coverage"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.1",
    "morgan": "^1.10.0",
    "puppeteer": "^24.15.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
